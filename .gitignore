.env
.env.*
!.env.example
secrets/
node_modules/
dist/
dist-electron/
dist-renderer/
win-unpacked/
*.log
backups/
backups-encrypted/
db/data/
volumes/db/data/

# The live install — the database itself, its WAL sidecars, the nightly
# snapshots and payment receipt uploads. Real financial records, never
# committed.
data/

# TypeScript incremental build cache.
*.tsbuildinfo

# Editor swap files.
*.swp
*.swo
*~
