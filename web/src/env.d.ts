// Substituted at build time by the `define` in vite.config.ts. Declared as a
// const rather than a var so a typo is a compile error rather than `undefined`
// rendered into the sidebar.
declare const __APP_VERSION__: string;
